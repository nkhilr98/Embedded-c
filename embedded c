BAZOOKAS!
TRAFFIC LIGHT SYSTEM USING ARDUINO

This model uses an Arduino and 8 LEDs to replicate a traffic light. It uses code as an internal timer and continues to run until the reset button is pressed. For 4 lanes, four red and four green LEDs are used to indicate STOP and GO signals. While the lane 1 is in green signal for 15 seconds, the lane 2 should have red signal for 15 seconds, the lane 3 should have red signal for 30 seconds and the lane 4 should have red signal for 45 seconds and this goes in a loop for all the four lanes.

CODE:

int r1=2,g1=3,r2=4,g2=5,r3=6,g3=7,r4=8,g4=9,rs=10;
int i;
void setup() {
  // put your setup code here, to run once:
pinMode(r1,OUTPUT);
pinMode(g1,OUTPUT);
pinMode(r2,OUTPUT);
pinMode(g2,OUTPUT);
pinMode(r3,OUTPUT);
pinMode(g3,OUTPUT);
pinMode(r4,OUTPUT);
pinMode(g4,OUTPUT);
pinMode(rs,OUTPUT);
}




void loop() {
// put your main code here, to run repeatedly:
for(i=0;i<60;i++)
{
      if(i<15)
      {
        digitalWrite(g1,HIGH);
        digitalWrite(r1,LOW);
        digitalWrite(g2,LOW);
        digitalWrite(r2,HIGH);
        digitalWrite(g3,LOW);
        digitalWrite(r3,HIGH);
        digitalWrite(g4,LOW);
        digitalWrite(r4,HIGH);
        delay(1000);
    }
    if(i>15 && i<30)
     {
        digitalWrite(g1,LOW);
        digitalWrite(r1,HIGH);
        digitalWrite(g2,HIGH);
        digitalWrite(r2,LOW);
        digitalWrite(g3,LOW);
        digitalWrite(r3,HIGH);
        digitalWrite(g4,LOW);
        digitalWrite(r4,HIGH);
        delay(1000);
    }
   

 if(i>30 && i<45)
      {
        digitalWrite(g1,LOW);
        digitalWrite(r1,HIGH);
        digitalWrite(g2,LOW);
        digitalWrite(r2,HIGH);
        digitalWrite(g3,HIGH);
        digitalWrite(r3,LOW);
        digitalWrite(g4,LOW);
        digitalWrite(r4,HIGH);
        delay(1000);
    }
    if(i>45 && i<60)
      {
        digitalWrite(g1,LOW);
        digitalWrite(r1,HIGH);
        digitalWrite(g2,LOW);
        digitalWrite(r2,HIGH);
        digitalWrite(g3,LOW);
        digitalWrite(r3,HIGH);
        digitalWrite(g4,HIGH);
        digitalWrite(r4,LOW);
        delay(1000);
    }
    if(rs==1)
    i=0;
  }
}


OUTPUT:  
